# 构建产物 public path（Vite base）
# ./              独立静态包，可部署到任意目录（默认，便于移植）
# /assets/sphere/ 自定义 URL 前缀
AVATAR_PUBLIC_PATH=./

# build:deploy 复制目标（相对 agent-sphere-avatar/）
# PAI 默认：../server/web/chat/assets/avatar
AVATAR_DEPLOY_DIR=../server/web/chat/assets/avatar
